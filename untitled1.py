import requests
import json

"""
data = json.load(var) - load for json
with open('xxx') as f:
    data = json.load(f)

for x in data['key_name']:
    print(x)
    print(x['attr'])
    del x['attr'] - deletes keys from x

with open('new_xxx.json','w') as f:
    json.dump(data, f)
    json.dump(data, f, indent=2)

with urlopen("URL") as response:
    source = response.read()

print(source) - prints everything on the website

data = json.loads(source) - loads for string
print(json.dumps(data, indent=2))

 

"""